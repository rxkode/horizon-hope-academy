# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Database
sqlalchemy==2.0.31
asyncpg==0.29.0
alembic==1.13.2

# Cache
redis==5.0.7

# Validation & settings
pydantic==2.8.2
pydantic-settings==2.3.4
python-dotenv==1.0.1
email-validator==2.2.0

# Security (JWT + hashing)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9

# HTTP client (M-Pesa Daraja + Airtel)
httpx==0.27.0

# PDF generation — ReportLab only (WeasyPrint too heavy for free tier)
# WeasyPrint requires Cairo/Pango system libs (~150MB) — install separately on paid tier
reportlab==4.2.2

# M-Pesa / payments
cryptography==42.0.8

# RosarioSIS DB connection (synchronous)
psycopg2-binary==2.9.9
